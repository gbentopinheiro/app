services:
  migrate:
    container_name: ${MIGRATE_CONTAINER_NAME:-bentix-dev-migrate}
    build:
      context: ../../..
      dockerfile: infra/docker/app/Dockerfile
      target: migrator
      args:
        NEXT_PUBLIC_APP_ENV: local
        NEXT_PUBLIC_API_BASE_URL: ""
    restart: "no"
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    networks:
      - bentix-net

  web:
    container_name: ${WEB_CONTAINER_NAME:-bentix-dev-web}
    build:
      context: ../../..
      dockerfile: infra/docker/app/Dockerfile
      args:
        NEXT_PUBLIC_APP_ENV: ${NEXT_PUBLIC_APP_ENV}
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    env_file:
      - .env
    environment:
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NEXT_PUBLIC_APP_ENV: ${NEXT_PUBLIC_APP_ENV}
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}
    ports:
      - "${WEB_PORT:-3100}:3000"
    volumes:
      - bentix-dev-exports:/app/data/exports
    networks:
      - bentix-net

  api:
    container_name: ${API_CONTAINER_NAME:-bentix-dev-api}
    build:
      context: ../../..
      dockerfile: infra/docker/app/Dockerfile
      args:
        NEXT_PUBLIC_APP_ENV: local
        NEXT_PUBLIC_API_BASE_URL: ""
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    env_file:
      - .env
    environment:
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NEXT_PUBLIC_APP_ENV: local
      NEXT_PUBLIC_API_BASE_URL: ""
    ports:
      - "${API_PORT:-3101}:3000"
    volumes:
      - bentix-dev-exports:/app/data/exports
    networks:
      - bentix-net

  db:
    image: mariadb:11.4
    container_name: ${DB_CONTAINER_NAME:-bentix-dev-db}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MARIADB_DATABASE: ${DB_NAME}
      MARIADB_USER: ${DB_USER}
      MARIADB_PASSWORD: ${DB_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
    ports:
      - "${DB_PORT:-3307}:3306"
    volumes:
      - bentix-dev-db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "127.0.0.1", "-u${DB_USER}", "-p${DB_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    networks:
      - bentix-net

volumes:
  bentix-dev-exports:
    name: bentix-dev-exports
  bentix-dev-db-data:
    name: bentix-dev-db-data

networks:
  bentix-net:
    external: true
